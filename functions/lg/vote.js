const Discord = require("discord.js");

module.exports = {
	/**
	 * Système de vote
	 * @param {*} interaction L'interaction qui vote
	 * @param {*} member Le membre qui se fais voté
	 */
	async vote(interaction, member) {
		//vote format :
		//in an array: [user1,user2,user3]
		//in another array : [0,5,3]
	},
};
